% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/find_duplicates.R
\name{find_duplicates}
\alias{find_duplicates}
\title{Find and remove duplicate .bib files in a directory}
\usage{
find_duplicates(path = ".", recursive = FALSE, remove = FALSE)
}
\arguments{
\item{path}{Character string: directory path to search for `.bib` files
(default = current directory), or a character vector of file paths.}

\item{recursive}{Logical: should the search recurse into subdirectories?
(default = `FALSE`)}

\item{remove}{Logical: if `TRUE`, duplicate files are removed; if `FALSE`,
only a report is printed.}
}
\value{
Invisibly returns a list with elements:
  * `duplicates` – a list of groups of duplicate file paths.
  * `removed` – vector of file paths that were removed (if `remove = TRUE`).
}
\description{
Find and remove duplicate .bib files in a directory
}
\examples{
\dontrun{
# Check current directory for duplicate .bib files (report only)
find_duplicate_files()

# Remove duplicates in a subdirectory, including subfolders
find_duplicate_files("papers", recursive = TRUE, remove = TRUE)
}
}
